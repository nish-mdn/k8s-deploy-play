mode: daemonset
image:
  repository: otel/opentelemetry-collector-contrib
  tag: "0.110.0"

presets:
  logsCollection:
    enabled: true
    includeCollectorLogs: false
    storeCheckpoints: true
  kubernetesAttributes:
    enabled: false 
  # FIXED: Disable the preset so we can manually override the TLS settings
  kubeletMetrics:
    enabled: false

config:
  receivers:
    # FIXED: Manual Kubelet receiver configuration
    kubeletstats:
      endpoint: "https://${env:K8S_NODE_NAME}:10250"
      auth_type: "serviceAccount"
      insecure_skip_verify: true
    
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"
        http:
          endpoint: "0.0.0.0:4318"

  processors:
    k8sattributes:
      extract:
        metadata:
          - k8s.namespace.name
          - k8s.pod.name
          - k8s.pod.uid
          - k8s.node.name
          - k8s.container.name
          - k8s.deployment.name
          - k8s.replicaset.name
    
    memory_limiter:
      check_interval: 5s
      limit_percentage: 80
      spike_limit_percentage: 25

    batch:
      send_batch_size: 1024
      timeout: 5s

    resource:
      attributes:
        - key: k8s.cluster.name
          value: "self-managed-aws"
          action: upsert

  exporters:
    otlphttp/loki:
      endpoint: "http://loki-gateway.observability.svc:80/otlp"
      tls:
        insecure: true

    prometheusremotewrite/vm:
      endpoint: "http://victoriametrics-victoria-metrics-single-server.observability.svc:8428/api/v1/write"
      resource_to_telemetry_conversion:
        enabled: true

    otlp/tempo:
      endpoint: "tempo.observability.svc:4317"
      tls:
        insecure: true

  service:
    pipelines:
      logs:
        receivers: [filelog]
        processors: [k8sattributes, resource, memory_limiter, batch]
        exporters: [otlphttp/loki]
      metrics:
        # Added kubeletstats here to the metrics pipeline
        receivers: [otlp, kubeletstats]
        processors: [k8sattributes, resource, memory_limiter, batch]
        exporters: [prometheusremotewrite/vm]
      traces:
        receivers: [otlp]
        processors: [k8sattributes, resource, memory_limiter, batch]
        exporters: [otlp/tempo]

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

clusterRole:
  create: true
  rules:
    - apiGroups: [""]
      resources: ["pods", "namespaces", "nodes", "nodes/proxy", "services", "endpoints", "nodes/stats"]
      verbs: ["get", "list", "watch"]
    - apiGroups: ["apps"]
      resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
      verbs: ["get", "list", "watch"]